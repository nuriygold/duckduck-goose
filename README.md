# duckduck-goose

CLI helper for Goose Telegram pairing.

## Usage

Connect Goose to Telegram via one CLI command:

```bash
python3 check_goose_telegram_pairing.py --connect
```

Other modes:

```bash
python3 check_goose_telegram_pairing.py
python3 check_goose_telegram_pairing.py --fix
```